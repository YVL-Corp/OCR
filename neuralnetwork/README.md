This is a neural network capable of computing simple binary operations such as AND, OR, XOR, NAND & XNOR.

To execute the program enter the following command :

gcc main.c neural_network.c -o output -lm

Then :

./output
